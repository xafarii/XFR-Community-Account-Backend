# Django
DEBUG=True
SECRET_KEY=change-me-in-production
ALLOWED_HOSTS=localhost,127.0.0.1

# Database (leave blank locally for now — we'll use SQLite until Render)
DATABASE_URL=

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:3000

# Groq
GROQ_API_KEY=
